//import java.util.Scanner;
public class MathTutorial
{
  public static void main(String[] args)
    {
        //this code gets the highest value between the two numbers
        //int Results = Math.max(5, 10);
        //System.out.println(Results);

        //This code will get the lowest value between the two numbers
        //int lowestValue = Math.min(5, 10);
        //System.out.println("The lowest value when comparing 5 and 10 is " + lowestValue);


        //this is a code that helps you find the square root of a number
       /*Scanner scanner = new Scanner(System.in);
       System.out.println("Welcome to the Square Root Checker Application");
       System.out.println("Enter the number you want to check");
       double number1 = scanner.nextDouble(); //accept the users input
       double result = Math.sqrt(number1); //this stores the answer in the variable called result
       System.out.println("The square root of " + number1 + " is 64" + result ); //print the result on the screen
       scanner.close(); //close the scanner*/

       
       //The code below is used to get the absolute value(positive value)
       //System.out.println(Math.abs(-4.7));

    }   
    
    
}
